import 'package:flutter/material.dart';


class CoursesSection extends StatelessWidget {
	const CoursesSection();

	@override
	Widget build(BuildContext context) {
		return const Center(child: Text("Курси"));
	}
}
