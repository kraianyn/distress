import 'package:flutter/material.dart';


class LocationsSection extends StatelessWidget {
	const LocationsSection();

	@override
	Widget build(BuildContext context) {
		return const Center(child: Text("Локації"));
	}
}
