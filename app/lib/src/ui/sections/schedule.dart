import 'package:flutter/material.dart';


class ScheduleSection extends StatelessWidget {
	const ScheduleSection();

	@override
	Widget build(BuildContext context) {
		return const Center(child: Text("Розклад"));
	}
}
